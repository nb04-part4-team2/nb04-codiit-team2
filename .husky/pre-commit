echo "🧹 코드 스타일을 검사하는 중입니다..."

npx lint-staged || {
  echo ""
  echo "❌ 코드 스타일(Lint) 에러가 발생했습니다!"
  echo "👉 Prettier나 ESLint 오류를 먼저 해결해 주세요."
  exit 1
}

echo "🔍 타입 검사를 실행 중입니다..."

npm run typecheck:all || {
  echo ""
  echo "❌ 타입 체크(TypeScript)에 실패했습니다!"
  echo "👉 위 에러 로그를 확인하고, 타입 오류를 수정한 뒤 다시 커밋해 주세요."
  exit 1
}

echo "✅ 모든 검사를 통과했습니다. 커밋을 진행합니다! 🎉"